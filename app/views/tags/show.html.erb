<% @tag.gifs.each do |gif|%>
<%= link_to image_tag(gif.image_url), gif_path(gif.id) %><br>
Created on <%= gif.created_at.localtime.strftime("%I:%M%p on %m/%d/%Y") %><br> <% gif.tags.each do |tag| %> <%=link_to tag.name, tag_path(tag.id)%> <% end %>
<% end %>